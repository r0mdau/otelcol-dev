<source>
  @type  forward
  @id    input1
  @label @mainstream
  port  24224
  <transport tls>
    cert_path /fluentd/etc/certs/fluentd.crt
    private_key_path /fluentd/etc/certs/fluentd.pem
    private_key_passphrase otelcol-dev
  </transport>
</source>

<filter **>
  @type stdout
</filter>

<label @mainstream>
  <match **>
    @type stdout
  </match>
</label>